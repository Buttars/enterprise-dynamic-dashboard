import { DashboardOutletDirective } from './dashboard-outlet.directive';

describe('DashboardOutletDirective', () => {
  it('should create an instance', () => {
    const directive = new DashboardOutletDirective();
    expect(directive).toBeTruthy();
  });
});
